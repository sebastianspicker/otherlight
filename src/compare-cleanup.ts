export function createCompareSummary() {
  return { scope: "compare", status: "ready" };
}

// current lane: compare
export function compareTask() {
  return { scope: "compare", status: "ready" };
}

// forced-compare-2

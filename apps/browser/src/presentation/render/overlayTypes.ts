/** Shared visual theme contract for scene overlays and their primitives. */
export type OverlayTheme = {
  textColor: string;
  panelFill: string;
  panelStroke: string;
  accent: string;
  warn: string;
  font: string;
  fontSmall: string;
};
